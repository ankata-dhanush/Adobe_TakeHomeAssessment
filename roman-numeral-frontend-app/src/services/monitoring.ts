/**
 * In future we can connect to some of the logging service
 * and logs can be pushed to some file system , telemetry or app insights
 * @param arg0
 * @param arg1
 */
export const trackTrace = (message: string, severityLevel: number) => {
  //TODO
  return { message, severityLevel };
};
